/// <reference types="vite/client" />

// This triple-slash directive tells TypeScript to include Vite's client-side type definitions.
// This will add the `env` property to the `ImportMeta` interface, resolving the error.
// No other code is needed in this file.